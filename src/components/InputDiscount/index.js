import logRender from '../../hoc/logRender';
import withInputNumber from '../../hoc/withInputNumber';
import Discount from 'csssr-school-input-discount';

export default withInputNumber(logRender(Discount));
